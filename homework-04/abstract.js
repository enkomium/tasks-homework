// У стрелочных функций нет псевдомассива АГРУМЕНТ, для сбора всех Аргументов надо распылять! если нет параметров то просто пустые скоби.
// При передаче колбэк функции арументом, мы передаем ссылку на эту функцию, и потом уже ее можно вызвать там куда передали!
// Помнить что функция возвращает что-то когда у неее есть возврат
// Замыкание это возможность функции находящейся в другую функции получать доступ к параметрам РОДИТЕЛЬСКОЙ функции при ВОЗВРАТЕ!