// Напиши скрипт который просит посетителя ввести число в prompt до тех пор,
// пока посетитель на нажмет Cancel и каждый раз добавляет введенное значение к общей сумме.

// При загрузке страницы пользователю предлагается в prompt ввести число. Ввод добавляется к значению переменной total.
// Операция ввода числа продолжается до тех пор, пока пользователь не нажмет кнопку Cancel в prompt.
// После того как пользователь прекратил ввод нажав кнопку Cancel, показать alert со строкой 'Общая сумма чисел равна [сумма]'.

/* Алгоритм СВОИМИ СЛОВАМИ */
// 1. Делаем ЦИКЛ который выводит Модалку, после чего перезаписывает то что мы ввели в Модалку в нужную переменную.
// 2. ДЕлаем Проверку на Отмену (null). То есть если в prompt не выводится null то преобразовывай в Число.
// 3. И потом уже если выводится null то выводи значение total, и в конце ЗАВЕРШАЕМ ЦИКЛ!!!! (выходим из него)

// let total = 0;
// let message;

// РЕШЕНИЕ - 1
// for (let i = 0; i < 1; total += message) {
//   message = prompt('Введите любое число!');
//   if (message !== null) {
//     message = Number(message);
//   } else if (message === null) {
//     message = alert(`Общая сумма чисел равна ${total}`);
//     break;
//   }
// }

// РЕШЕНИЕ - 2
// for (let i = 0; i < 1; total += message) {
//   message = prompt('Введите любое число!');

//   if (message !== null) {
//     message = Number(message);
//   }

//   if (message === null) {
//     message = alert(`Общая сумма чисел равна ${total}`);
//     break;
//   }
// }
// --------------------------------------------------------------------------------------------------------------------------------

// СУПЕР РЕШЕНИЕ!!!
// СУПЕР-ПУПЕР РЕШЕНИЕ В САМОМ НИЗУ!!!)))

// let input;
// let total = 0;

// for(let i = 0; true; i += 1){
//     input = prompt('Введите число');

//     if(input === null){
//         alert(`Общая сумма чисел равна ${total}`);
//         break;
//     }else if (input.trim() === "") { // можно сделать !nput.trim() - типа trim возвращает пустую строку а пустая строка это false
//         alert("Вы ничего не ввели!");
//     }else if(isNaN(Number(input))) {
//         alert('Было введено не число, попробуйте еще раз');
//     }else if(input <= 0) {
//         alert("Введите число больше 0!");
//     }else if (input.startsWith('0') && input.length > 1) {
//         alert("Введите число без ведущих нулей!");
//     }else {
//         input = Number(input);
//         total += input;
//     }
    
// }

// КАК РАБОТАЕТ КОД!!!
// Инициализация переменных:
// let input; — переменная для хранения введенного значения.
// let total = 0; — переменная для хранения общей суммы введенных чисел.

// Бесконечный цикл:
// for(let i = 0; true; i += 1) — цикл, который будет выполняться бесконечно, пока не будет вызван break.

// Запрос ввода:
// input = prompt('Введите число'); — запрашивает у пользователя ввод числа.

// Проверка на "Отмена":
// if(input === null) — если пользователь нажал "Отмена", выводится общая сумма и цикл прерывается.

// Проверка на пустую строку:
// else if (input.trim() === "") — если введена пустая строка, выводится сообщение об ошибке.

// Проверка на нечисловое значение:
// else if(isNaN(Number(input))) — если введенное значение не является числом, выводится сообщение об ошибке.

// Проверка на число меньше или равное нулю:
// else if(input <= 0) — если введенное число меньше или равно нулю, выводится сообщение об ошибке.

// Проверка на ведущие нули:
// else if (input.startsWith('0') && input.length > 1) — если строка начинается с '0' и длина больше 1, выводится сообщение об ошибке.

// Суммирование введенных чисел:
// Если все проверки пройдены, ввод преобразуется в число и добавляется к общей сумме.
// --------------------------------------------------------------------------------------------------------------------------------------

// ПСЕВДОКОД ДЛЯРЕШЕНИЯ ЭТОЙ ЗАДАЧИ!!!

// Инициализировать total = 0

// Делаем бесконечный Цикл Пока истинно:
//     input = prompt("Введите число")

//     Если input равно null:
//         alert("Общая сумма чисел равна " + total)
//         выйти из цикла

//     Иначе если input пустая строка:
//         alert("Вы ничего не ввели!")

//     Иначе если input не число:
//         alert("Было введено не число, попробуйте еще раз")

//     Иначе если input меньше или равно 0:
//         alert("Введите число больше 0!")

//     Иначе если input начинается с '0' и длина больше 1:
//         alert("Введите число без ведущих нулей!")

//     В противном случае:
//         input = Number(input)
//         total += input

// ---------------------------------------------------------------------------------------------------------------------------
// СУПЕР-ПУПЕР РЕШЕНИЕ!!!
// let input;
// let total = 0;

// while(true){
//     input = prompt('Введите число!');

//     if(input === null){
//         alert(`Общая сумма чисел равна ${total}`);
//         break;
//     }else if(input.trim() === ''){ // можно сделать !nput.trim() - типа trim возвращает пустую строку а пустая строка это false
//         alert('Продолжайте вводить числа пока не нажмете "отмена"!');
//         continue; //не обязательно
//     }else if(input.startsWith('0') && input.length > 1){
//         alert('Введите число без ведущих нулей!');
//     }else {

//         input = Number(input);

//         if(isNaN(input)){
//             alert('Введите корректное число!');
//         }else if(input <= 0){
//             alert('Введите чило больше нуля!');
//         }else {
//             total += input;
//         }
//     }
// }