// На счету пользователя есть 23580 кредитов, значение хранится в переменной credits(создай и присвой).
// Пользователь решает купить ремонтных дроидов, которые стоят по 3000 кредитов за штуку.
// Цена одного дроида хранится в переменной pricePerDroid(создай и присвой).

// При посещении страницы, используя prompt, необходимо спросить,
// какое количество дроидов которые пользователь хочет купить и сохранить в переменную.

// Напиши скрипт который:

// Если в prompt была нажата кнопка Cancel, выводит в консоль сообщение 'Отменено пользователем!'.
// В противном случае, рассчитывает общую цену заказа и сохраняет в переменной totalPrice.
// Проверяет сможет ли пользователь оплатить заказ:
// если сумма к оплате превышает количество кредитов на счету, выводи в консоль сообщение 'Недостаточно средств на счету!'.
// в противном случае необходимо посчитать остаток кредитов на счету
// и вывести сообщение 'Вы купили [число] дроидов, на счету осталось [число] кредитов.'.

// let credits = 23580;
// const pricePerDroid = 3000;

// let totalPrice = 0;

// const message = prompt('Сколько дроидов вы хотите купить?');

// totalPrice = message * pricePerDroid;

// if (message === null) {
//   message = alert('Отменено пользователем!');
// } else if (totalPrice > credits) {
//   message = alert('Недостаточно средств на счету!');
// } else if (totalPrice <= credits) {
//   credits -= totalPrice;
//   message = alert(
//     `Вы купили ${message} дроидов, на счету осталось ${credits} кредитов.`,
//   );
// }

// ---------------------------------------------------------------------------------------------------------------------------------------

// Более Грамотное Решение!!!
// Задание 4
// На счету пользователя есть 23580 кредитов, значение хранится в переменной credits (создай и присвой). 
// Пользователь решает купить ремонтных дроидов, которые стоят по 3000 кредитов за штуку. 
// Цена одного дроида хранится в переменной pricePerDroid (создай и присвой).

// При посещении страницы, используя prompt, 
// необходимо спросить количество дроидов которые пользователь хочет купить и сохранить в переменную.

// Напиши скрипт который:

// Если в prompt была нажата кнопка Cancel, выводит в консоль сообщение 'Отменено пользователем!'.
// В противном случае, рассчитывает общую цену заказа и сохраняет в переменной totalPrice.
// Проверяет сможет ли пользователь оплатить заказ:
// если сумма к оплате превышает количество кредитов на счету, выводи в консоль сообщение 'Недостаточно средств на счету!'.
// в противном случае необходимо посчитать остаток кредитов на счету и вывести сообщение 
// 'Вы купили [число] дроидов, на счету осталось [число] кредитов.'.

// Алгоритм решения:
// Сделали Промпт
// Так как промт возвращает СТРОКУ вначале делаем банальные проверки на отмену, пустой ввод и символ 0 в начале текста.
// После этих проверок приводим введенное значение к ЧИСЛУ!
// ДАлее делаем проверку на число
// Потом Проверку на число меньше 0!
// И проверку на ЦЕЛОЕ чсло!
// После чего умножаем введенное значение на стоимость единицы товара и записываем в переменную!
// Потом делаем проверку где сравниваем итоговую сумму с суммой кредитов
// Если если сумма меньше или равна сумме Кредитов, отнимаем с кредитов сумму покупки и выводим Окно об успешной покупке.
// В противном случае выводим окно что Средств не достаточно!

// То есть сперва делаем проверки по типу строк а потом проверки с числами!

// let credits = 23580;
// const pricePerDroid = 3000;

// let totalPrice = 0;

// while(true){
//     let input = prompt(`Сколько дроидов вы хотите купить?
//     \nНа вашем счете ${credits} кредитов\nЦена одного дроида ${pricePerDroid} кредитов`);
        
//         if(input === null){
//             alert('Отменено пользователем!');
//         }else if(input.trim() === ''){
//             alert('Вы не чего не ввели!');
//         }else if(input.startsWith('0') && input.length > 1){
//             alert('Введите число без ведущих нулей!');
//         }else {
//             input = Number(input);
        
//             if(isNaN(input)){
//                 alert('Введите число!');
//             }else if(input <= 0){
//                 alert('Введите число больше нуля!');
//             }else if(!Number.isInteger(input)){
//                 alert('Введите целое число!');
//             }else {
//                 totalPrice = input * pricePerDroid;
        
//                 if(!confirm(`Вы действительно хотите купить ${input} дроида/ов за ${totalPrice} кредитов?`)){
//                     alert('Отменено пользователем!');
//                 }else {
//                     if(totalPrice <= credits){
//                         credits -= totalPrice;
//                         alert(`Вы купили ${input} дроидов, на счету осталось ${credits} кредитов.`);
//                         break;
//                     }else {
//                         alert('Недостаточно средств на счету!');
//                     }
//                 }
        
//             }
//         }
// }

// ПРОВЕРКА НА ВЕДУЩИЕ НУЛИ!
// Эта проверка добавляется после того, как вы проверили, что quantity не равен null и не является числом!!!!

// quantity.startsWith('0'):
// Этот метод проверяет, начинается ли строка quantity с символа '0'.
// Например, если quantity равно '01', то startsWith('0') вернет true.
// quantity.length > 1:
// Этот метод возвращает длину строки quantity.
// Проверка length > 1 нужна для того, чтобы убедиться, что строка не просто состоит из одного символа '0', 
// а содержит больше одного символа (например, '01', '001' и т.д.).
// Если quantity равно '0', то его длина будет 1, и условие length > 1 вернет false, что предотвратит срабатывание этого условия.
// Эта проверка позволяет убедиться, что пользователь не вводит числа с ведущими нулями, кроме случая, когда число — это просто '0'.

// ПРОВЕРКА НА ПУСТОЙ ВВОД!!!
// Проверку на пустой ввод следует делать сразу после того, как вы получили значение из prompt и перед тем, 
// как вы начнете выполнять другие проверки. Это важно, чтобы избежать ненужных преобразований и ошибок.

// ОБЪЯСНЕНИЕ ЛОГИКИ!!!!!
// Проверка на null:
// Если пользователь нажал "Отмена", выводится сообщение "Отменено пользователем!".

// Проверка на пустую строку:
// Если пользователь ничего не ввел (например, только пробелы), выводится сообщение "Вы ничего не ввели!".

// Проверка на ведущие нули:
// Если строка начинается с '0' и длина строки больше 1, выводится сообщение "Введите число без ведущих нулей!".

// Преобразование в число:
// После всех строковых проверок quantity преобразуется в число.

// Проверка на NaN:
// Если введенное значение не является числом, выводится сообщение "Введите число!".

// Проверка на положительное значение:
// Если число меньше или равно нулю, выводится сообщение "Введите число больше 0!".

// Проверка на целое число:
// Если введенное значение не является целым числом, выводится сообщение "Введите целое число!".

// Расчет и вывод:
// Если все проверки пройдены, рассчитывается totalPrice. 
// Если на счету достаточно средств, происходит покупка, и выводится сообщение с оставшимся балансом. 
// Если средств недостаточно, выводится сообщение "Недостаточно средств на счету!".

// ПРО +=
// Используйте +=, если вы хотите накапливать значения (например, если пользователь может совершать несколько покупок).
// Используйте простое присваивание =, если вам нужно, чтобы переменная отражала только текущее значение (например, стоимость одной покупки).